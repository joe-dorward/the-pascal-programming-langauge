PROGRAM integer_division(INPUT,OUTPUT);

{ this is a small test program }

BEGIN
  WRITE('-15 divided by two is: "',-15 DIV 2);
  WRITELN('" Remainder: "',-15 MOD 2,'"');
END.   { program integer_division }
