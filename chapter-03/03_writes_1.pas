PROGRAM writes_1(INPUT,OUTPUT);

{ this program shows difference between
  WRITE and WRITELN }

BEGIN
  WRITELN('================');
  WRITE;
  WRITELN;
  WRITELN('================');
END.   { program writes_1 }
